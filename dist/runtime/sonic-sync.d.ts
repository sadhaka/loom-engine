export declare const FP_SHIFT = 16;
export declare const FP_ONE: number;
export declare const FP_HALF: number;
export declare const ATTENUATION_FULL = 255;
export declare const ATTENUATION_NONE = 0;
export declare const TRACE_INAUDIBLE = -1;
export declare const SOURCE_SLOT_INVALID = -1;
export declare const LISTENER_SLOT_INVALID = -1;
export declare const PERCEPTION_EVENT_STRIDE = 6;
export interface SonicSyncConfig {
    maxSources: number;
    maxListeners: number;
    voxelGridSize: number;
    maxRayLength: number;
    maxSemanticId: number;
    eventCapacity: number;
    cooldownTicks: number;
}
export declare class SonicSync {
    readonly maxSources: number;
    readonly maxListeners: number;
    readonly voxelGridSize: number;
    readonly maxRayLength: number;
    readonly maxSemanticId: number;
    readonly eventCapacity: number;
    readonly cooldownTicks: number;
    readonly cooldownTableSize: number;
    private readonly cooldownMask;
    private readonly sourcePositionX;
    private readonly sourcePositionY;
    private readonly sourcePositionZ;
    private readonly sourceSemanticId;
    private readonly sourceIntensity;
    private readonly sourceActive;
    private sourceCount;
    private readonly listenerPositionX;
    private readonly listenerPositionY;
    private readonly listenerPositionZ;
    private readonly listenerHearingRadius;
    private readonly listenerSemanticMask;
    private readonly listenerActive;
    private listenerCount;
    private readonly voxelGrid;
    private readonly eventRing0;
    private readonly eventRing1;
    private frontIsRing0;
    private frontCount;
    private backCount;
    private eventsDroppedTotal;
    private readonly cooldownKeyHi;
    private readonly cooldownKeyLo;
    private readonly cooldownTick;
    private cooldownEntryCount;
    private readonly octantStepX;
    private readonly octantStepY;
    private readonly octantStepZ;
    private readonly rayDirection;
    private currentTick;
    constructor(config: SonicSyncConfig);
    getSourceCount(): number;
    getListenerCount(): number;
    getEventsDroppedTotal(): number;
    getFrontEventCount(): number;
    getBackEventCount(): number;
    getCurrentTick(): number;
    getCooldownEntryCount(): number;
    addSource(positionX: number, positionY: number, positionZ: number, semanticId: number, intensity: number): number;
    updateSource(slot: number, positionX: number, positionY: number, positionZ: number, intensity: number): boolean;
    deactivateSource(slot: number): boolean;
    addListener(positionX: number, positionY: number, positionZ: number, hearingRadius: number, semanticMask: number): number;
    updateListener(slot: number, positionX: number, positionY: number, positionZ: number, hearingRadius: number, semanticMask: number): boolean;
    deactivateListener(slot: number): boolean;
    setVoxel(x: number, y: number, z: number, occlusion: number): boolean;
    getVoxel(x: number, y: number, z: number): number;
    fillVoxelRegion(x0: number, y0: number, z0: number, x1: number, y1: number, z1: number, occlusion: number): void;
    traceOcclusion(sourceSlot: number, listenerSlot: number): number;
    private dda;
    producePerceptionEvents(): number;
    tick(t: number): void;
    readEvent(i: number, out: Int32Array, outOffset?: number): boolean;
    private cooldownAllow;
    private cooldownStamp;
    private cooldownProbe;
    private inGrid;
    private voxelIndex;
    private requireSourceSlot;
    private requireListenerSlot;
    private requireVoxelCoords;
    private backIsRing0;
    clear(): void;
}
//# sourceMappingURL=sonic-sync.d.ts.map